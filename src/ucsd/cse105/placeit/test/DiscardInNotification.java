package ucsd.cse105.placeit.test;
/*
import static org.junit.Assert.fail;

import org.junit.Before;
import org.junit.Test;

import com.robotium.solo.Solo;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class DiscardInNotification extends BroadcastReceiver {

	private Solo solo;
	
	@Before
	public void setUp() throws Exception {
		//super.setUp();
		solo = new Solo(null);
	}

	@Test
	public void test() {
		fail("Not yet implemented");
	}

	@Override
	public void onReceive(Context arg0, Intent arg1) {
		// TODO Auto-generated method stub
		//super.onReceive(arg0, arg1);	
	}
	/*
	//solo.clickOnButton("Create");
	//solo.enterText(0, <tt>"DiscardInNotification");
	//solo.enterText(1,</tt> "User Story 3 testPulledDown");
	//solo.enterText(2,"-30,-20");
	//solo.enterText(3,"Blue");
	//solo.clickOnButton("One Week");
	//solo.hideSoftKeyboard();
	//solo.clickOnButton("Create the PlaceIt");
	
	//solo.clickOnButton("Pulled");
	//solo.clickOnText("DeletePulledDown");
	//solo.clickOnButton("Delete");
	//solo.takeScreenshot();
	//boolean deleted = solo.searchText("DeletePulledDown");
	//assertFalse("Delete failed",deleted);
	
	//solo.goBack();
	//solo.clickOnButton("Active");
	//solo.takeScreenshot();
	//boolean activeDelete = solo.searchText("DeletePulledDown");
    assertFalse("Delete from Active List failed",activeDelete);
    
    solo.clickOnButton("Create");
    solo.enterText(0, tt>"DiscardInNotification");
    solo.enterText(1, tt>"User Story 3 testPullDown");
    solo.enterText(2, "-30, -20");
    solo.enterText(3, "Blue");
    solo.clickOnButton("One Week");
    solo.hideSoftKeyboard();
    solo.clickOnButton("Create the Placeit");
    
    solo.clickOnButton("Pulled");
    solo.clickOnText("DeletePullDown");
    solo.clickOnButton("Discard");
    solo.takeScreenshot();
    boolean deleted = solo.searchText("DeletePullDown");
    assertFalse("Delete failed", deleted);
    
	solo.goBack();
	solo.clickOnButton("Active");
	solo.takeScreenshot();
	boolean activeDelete = solo.searchText("DeletePulledDown");
    assertFalse("Delete from Active List failed",activeDelete);
    

}
*/